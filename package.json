{
  "private": true,
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "check": "biome check static/",
    "check:ci": "biome ci static/",
    "format": "biome check --write static/",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@biomejs/biome": "^1",
    "@playwright/test": "^1.50",
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3"
  }
}
